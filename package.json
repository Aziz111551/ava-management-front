{
  "name": "multiple-files-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix worksphere",
    "build": "npm run build --prefix worksphere",
    "preview": "npm run preview --prefix worksphere"
  }
}
